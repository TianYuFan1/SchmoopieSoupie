// Specifies initial grid dimension
GRID_DIM = 50;
// Creates the initial grid
create_grid(GRID_DIM);
// Initiates key bindings (S, W, E)
init_state_keys();
// Initiates the search button
init_search_button();
